using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace App_Ecommerce.Services.Product
{
    public class ProductService /*: IProductService*/
    {
        /*private readonly ProductManager<Product> productManager;

        public Task SetCurrentProductImageUrl(string url)
        {
            if (string.IsNullOrEmpty(url))
                throw new ArgumentException("Url is missing");

            var product = url;

            await productManager.UpdateAsync(product);

        }*/

   
    }
}
